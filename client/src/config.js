// ---------------------------------------------------------------------------
// Brand configuration — single source of truth for the restaurant name.
// To rebrand the whole app, change ONLY these values (and, if you want the
// browser tab / installed-app name to match, index.html + manifest.webmanifest).
// ---------------------------------------------------------------------------
export const BRAND = {
  name: "Zafran",
  tagline: "Modern Kitchen",
  // Currency prefix shown next to prices (e.g. "QR", "AED", "₹", "$").
  currency: "QR",
  logo: "/logo.svg",
};
